# ── Postgres (read-only usage — same DB the KB platform uses) ────────────────
DATABASE_SYNC_URL=postgresql://slm:slmpass@localhost:5432/slmdb

# ── Better Stack (Logtail) ────────────────────────────────────────────────────
# From your Better Stack "Connect source" page (HTTP source type):
BETTERSTACK_SOURCE_TOKEN=
BETTERSTACK_INGEST_HOST=

# ── Mock server behavior ──────────────────────────────────────────────────────
MOCKPROD_ERROR_RATE=0.15

# ── Error simulator (simulate_errors.py) ─────────────────────────────────────
MOCKPROD_BASE_URL=http://localhost:8100
SIM_DURATION_S=60
SIM_MALFORMED_RATE=0.3
SIM_RPS=1
